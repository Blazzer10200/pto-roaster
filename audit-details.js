export function auditDocument(actorName,document){let details=null;try{details=typeof document==='string'?JSON.parse(document):document;}catch{}return JSON.stringify({actorName:actorName||'Site operator',details});}
export function auditDetails(document){let d;try{d=JSON.parse(document);}catch{return {};}if(!d)return {};const details=d.details??d,changes=[];if(Array.isArray(details.previousRoles))changes.push('Website roles: '+(details.previousRoles.join(', ')||'none')+' → '+(details.roleIds?.join(', ')||'none'));if(typeof details.previousDisabled==='boolean')changes.push('Sign-in: '+(details.previousDisabled?'disabled':'enabled')+' → '+(details.disabled?'disabled':'enabled'));if(details.before?.roles&&details.after?.roles){for(const role of details.after.roles){const old=details.before.roles.find(r=>r.id===role.id);if(JSON.stringify(old)!==JSON.stringify(role))changes.push((old?'Updated role: ':'Added role: ')+role.name);}for(const role of details.before.roles)if(!details.after.roles.some(r=>r.id===role.id))changes.push('Removed role: '+role.name);if(JSON.stringify(details.before.categories)!==JSON.stringify(details.after.categories))changes.push('Navigation categories changed; moved pages retain prior role permissions.');}return {actorName:d.actorName,changes};}
